[buildout]
parts = django
index= http://d.pypi.python.org/simple
develop = .
versions = versions

[versions]
version = 1.3

[django]
recipe = djangorecipe
settings = settings
wsgi = true
project = src
extra-paths = src
eggs = oauth
	   django_compressor
